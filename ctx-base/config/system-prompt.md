Replace this file with what your agent should be told before every session,
or delete it. It is appended to Claude Code's own system prompt, so write
what is true of the agent rather than of one task, and keep it short: it is
paid on every turn.

You work on a payment router. Amounts are always in minor units.
