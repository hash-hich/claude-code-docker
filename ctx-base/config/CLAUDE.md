# Example memory

Replace this file with the instructions every session of your agent should
start with, or delete it. It is loaded on every turn, so keep it to what is
true of every task: the shape of the repository, the commands that build and
test it, the conventions a reviewer would otherwise have to repeat.

- Run the test suite before proposing a change.
- Keep commits to one decision each.
